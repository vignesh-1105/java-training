public class arrayaddition {
    public static void main(String[]args){
        int[] arr = {10,20,30,40,50,60};
        for (int i = 1; i < arr.length; i++) {
     arr[i] = arr[i] + arr[i-1];
        }
        System.out.print("array addition is:");
         for (int i = 0; i < arr.length; i++) {
        System.out.print(arr[i] + " ");
        }
    }
}        
